{
  "name": "Labortablo",
  "description": "Labortablo Application",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "pm2": "^5.4.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.10.0",
    "eslint": "^9.10.0",
    "globals": "^15.9.0",
    "handlebars": "^4.7.8",
    "minify": "^11.4.1",
    "nodemon": "^3.1.7",
    "prettier": "^3.3.3"
  },
  "scripts": {
    "dev": "npx pm2 -s --env development --no-daemon restart ./ecosystem.development.config.js --update-env",
    "production": "npx pm2 -s --env production --no-daemon restart ./ecosystem.config.js --update-env"
  }
}
